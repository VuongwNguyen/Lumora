name: Deploy to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            source ~/.nvm/nvm.sh 2>/dev/null || true
            source ~/.profile 2>/dev/null || true
            cd ${{ secrets.VPS_APP_PATH }}
            # ${{ github.repository }} tự đổi theo tên repo, nên đổi tên trên
            # GitHub không cần sửa file này và không phụ thuộc redirect tên cũ.
            git pull https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git main
            yarn install --frozen-lockfile
            pm2 restart galaxy
            sleep 3 && curl -sf http://localhost:3030/ping > /dev/null
